# Plugin-based-PHP-MVC-Framework
we create a PHP MVC Framework that uses plugins to help you start new projects faster instead of having to start from scratch on every project. It combines the advantages of wordpress and light frameworks like codeigniter, creating an MVC Framework that uses plugins.
